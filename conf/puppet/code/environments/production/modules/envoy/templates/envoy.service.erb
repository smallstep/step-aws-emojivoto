[Unit]
Description=Envoy proxy
Wants=basic.target
After=basic.target network.target

[Service]
WorkingDirectory=/usr/local/lib/envoy
ExecStart=<%= scope["exec_start"] %>
KillMode=process
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
